import { Address } from "@rarible/types";
import { Word } from "@rarible/types";
export declare type LogEvent = {
    transactionHash: Word;
    status: LogEvent_Status;
    address: Address;
    from?: Address;
    topic: Word;
    nonce?: number;
};
export declare type LogEvent_Status = "PENDING" | "CONFIRMED" | "REVERTED" | "DROPPED" | "INACTIVE";
